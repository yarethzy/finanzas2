// informe.dart

import 'package:flutter/material.dart';

class InformeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Informe'),
      ),
      body: Center(
        child: Text('Contenido del Informe'),
      ),
    );
  }
}
